int main()
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
    int main()
{
    // Test case
    char inputString[] = "Hello, World!";
    char oldChar = 'l';
    char newChar = 'z';

    char expectedResult[] = "Hezzo, Worzd!";

    replaceChar(inputString, oldChar, newChar);

    if (areStringsEqual(inputString, expectedResult))
    {
        printf("Test Passed! Result: %s\n", inputString);
    }
    else
    {
        printf("Test Failed! Result: %s\n", inputString);
    }

    


const char *inputString = "Hello, World!";
char oldChar = 'l';
char newChar = 'z';

const char *expectedResult = "Hezzo, Worzd!";

char *result = replaceCharCopy(inputString, oldChar, newChar);

if (areStringsEqual(result, expectedResult) )
{
    printf("Test Passed! Result: %s\n", result);
}
else
{
    printf("Test Failed! Result: %s\n", result);
}









 char inputString[] = "Hello, World!";
    char charToRemove = 'l';

   
    const char *expectedResult = "Heo, Word!";

   
    removeChar(inputString, charToRemove);

    
    if (areStringsEqual(inputString, expectedResult)) {
        printf("Test Passed! Result: %s\n", inputString);
    } else {
        printf("Test Failed! Result: %s\n", inputString);
    }